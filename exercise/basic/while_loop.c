#include <stdio.h>

int main() {
  int i = 1; // Initialize the counter variable

  while (i <= 10) { // Condition: loop continues as long as i is less than or equal to 10
    printf("%d ", i); // Print the current value of i
    i++; // Increment i to the next number
  }

    printf("\n"); // Print a newline character for formatting

  return 0;
}